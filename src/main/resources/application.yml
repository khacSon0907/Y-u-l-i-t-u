server:
  port: 8080

spring:
  application:
    name: demo

  redis:
    host: localhost
    port: 6379
    timeout: 60000ms

  data:
    mongodb:
      uri: mongodb+srv://cakeyummy543:gT7zl5A6Gjqn5vwK@demodb.gjvjuqt.mongodb.net/demoDB?retryWrites=true&w=majority&appName=demoDB

  mail:
    host: smtp-relay.brevo.com
    port: 587
    username: 9ffd87001@smtp-brevo.com
    password: bska1sH3jPyoZ16   # phải là key FULL, không phải ********
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true


jwt:
  secret: my-super-secret-key-my-super-secret-key
  access-token-expiration: 900000        # 15 phút
  refresh-token-expiration: 604800000    # 7 ngày
  verify-token-expiration: 900000

app:
  backend-url: http://localhost:8080
  mail:
    from: " TroSmart <creatine09072004@gmail.com> "
    verify-endpoint: /api/authService/verify

  cookie:
    # Development: false (HTTP), Production: true (HTTPS only)
    secure: false

    # Domain cho cookie (để localhost khi dev)
    domain: localhost

    # Access token cookie max age (seconds) - 15 phút
    access-token-max-age: 900

    # Refresh token cookie max age (seconds) - 7 ngày
    refresh-token-max-age: 604800
